"""Stage 0 (preflight) and Stage 6 (outcome feedback).

Stage 0 is optional (``--skip-preflight``) and evidence-driven: (1) the reactor's own upload
history via the outcome table (outcomes.py); (2) YouTube at large (surviving long-form reactions to
the same title). Output: green/yellow/red flag + monetization-likelihood note. Red requires override.
"""
